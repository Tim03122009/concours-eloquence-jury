{
  "name": "concours-eloquence-functions",
  "description": "Cloud Functions pour le vote public (vérification par IP)",
  "main": "index.js",
  "type": "module",
  "engines": { "node": "18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0"
  }
}
